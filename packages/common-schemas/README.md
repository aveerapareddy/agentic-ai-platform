# common-schemas

Shared Pydantic v2 models for execution, workflow, tooling, policy, and feedback contracts.

Install (editable): `pip install -e packages/common-schemas` from the monorepo root.

Import: `from common_schemas import Execution, ToolCall, PolicyEvaluation, ...`
