name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      # TODO: install Python/Node, lint, unit tests, integration subset
      - run: echo "TODO wire CI"
